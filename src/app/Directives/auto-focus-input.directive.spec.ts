import { AutoFocusInputDirective } from './auto-focus-input.directive';

describe('AutoFocusInputDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoFocusInputDirective();
    expect(directive).toBeTruthy();
  });
});
